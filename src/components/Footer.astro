---
const today = new Date();
import pkg from '../../package.json';

// Git commit hash (set during build)
const gitHash = import.meta.env.PUBLIC_GIT_HASH || 'dev';
const version = pkg.version;
---

<footer>
	<div class="footer-wrapper">
		<p class="copyright">
			&copy; 2024-{today.getFullYear()} Cristea Florian-Victor. Toate drepturile rezervate.
		</p>

		<p class="footer-note">
			Acest instrument a fost realizat de Cristea Florian Victor pentru fiecare arbitru AJF Ilfov.  
			Verificați mereu corectitudinea rapoartelor generate de acest program web.
		</p>
		<p class="footer-note">Găzduit de Cloudflare</p>
		<p class="version-info">v{version} · {gitHash}</p>
	</div>
</footer>

<style>
	footer {
		width: 100%;
		padding: clamp(2rem, 4vw, 3rem) 1rem;
		background: var(--ui-card);
		border-top: 1px solid var(--ui-border);
		margin-top: auto;
		display: flex;
		justify-content: center;
		align-items: center;
		transition: background-color 0.3s ease, border-color 0.3s ease;
	}

	.footer-wrapper {
		max-width: 900px;
		width: 90%;
		display: flex;
		flex-direction: column;
		align-content: center
		align-items: center;
		text-align: center;
		gap: 0.7rem;
		line-height: 1.6;
	}

	.footer-wrapper p {
		margin: 0;
		padding: 0;
	}

	.copyright {
		font-size: 0.9375rem;
		font-weight: 500;
		color: var(--ui-text);
	}

	.footer-note {
		font-size: 0.875rem;
		color: var(--ui-muted);
		max-width: 700px;
	}

	.version-info {
		font-size: 0.75rem;
		color: var(--ui-muted);
		font-family: 'Monaco', 'Courier New', monospace;
		opacity: 0.75;
		margin-top: 0.5rem;
	}

	.version-info:hover {
		opacity: 1;
	}

	@media (max-width: 639px) {
		
		footer {
			padding: 1.5rem 1rem;
		}

		.copyright {
			font-size: 0.875rem;
		}

		.footer-note {
			font-size: 0.8125rem;
		}
		
		.version-info {
			font-size: 0.6875rem;
		}
	}
</style>
