---
const today = new Date();
import pkg from '../../package.json';

// Git commit hash (set during build)
const gitHash = import.meta.env.PUBLIC_GIT_HASH || 'dev';
const version = pkg.version;
---

<footer>
	<div class="footer-content">
		<div class="copyright">
			&copy; 2024-{today.getFullYear()} Cristea Florian-Victor. Toate drepturile rezervate.
		</div>
		<div class="footer-note">
			Acest instrument a fost realizat de Cristea Florian Victor pentru fiecare arbitru AJF Ilfov. Verificați mereu corectitudinea rapoartelor generate de acest program web.
		</div>
		<div class="footer-links">
			<a href="/termeni-si-conditii">Termeni și Condiții</a>
		</div>
		<div class="footer-note">
			Găzduit de Cloudflare
		</div>
		<div class="version-info">
			v{version} · {gitHash}
		</div>
	</div>
</footer>

<style>
	footer {
		width: 100%;
		padding: clamp(2rem, 4vw, 3rem) 0;
		background: var(--ui-card);
		border-top: 1px solid var(--ui-border);
		margin-top: auto;
		transition: background-color 0.3s ease, border-color 0.3s ease;
	}

	.footer-content {
		max-width: 1400px;
		width: 100%;
		margin: 0 auto;
		padding: 0 clamp(1rem, 3vw, 2rem);
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.5rem;
	}

	.copyright {
		margin: 0;
		padding: 0;
		font-size: 0.9375rem;
		font-weight: 500;
		color: var(--ui-text);
		transition: color 0.3s ease;
		text-align: center;
		width: 100%;
	}

	.footer-note {
		margin: 0;
		padding: 0;
		font-size: 0.875rem;
		color: var(--ui-muted);
		transition: color 0.3s ease;
		line-height: 1.6;
		text-align: center;
		width: 100%;
		max-width: 800px;
	}

	.version-info {
		margin: 0.25rem 0 0;
		padding: 0;
		font-size: 0.75rem;
		color: var(--ui-muted);
		font-family: 'Monaco', 'Courier New', monospace;
		opacity: 0.7;
		transition: color 0.3s ease, opacity 0.3s ease;
		text-align: center;
		width: 100%;
	}

	.version-info:hover {
		opacity: 1;
	}

	.footer-links {
		margin: 0.5rem 0;
		padding: 0;
		text-align: center;
		width: 100%;
	}

	.footer-links a {
		color: var(--ui-primary);
		text-decoration: none;
		font-size: 0.875rem;
		font-weight: 500;
		transition: color 0.3s ease, text-decoration 0.3s ease;
	}

	.footer-links a:hover {
		text-decoration: underline;
		color: var(--ui-primary-hover);
	}

	@media (max-width: 639px) {
		footer {
			padding: 1.5rem 0;
		}
		
		.footer-content {
			padding: 0 1rem;
		}

		.copyright {
			font-size: 0.875rem;
			line-height: 1.5;
		}

		.footer-note {
			font-size: 0.8125rem;
		}

		.version-info {
			font-size: 0.6875rem;
		}
	}
</style>
